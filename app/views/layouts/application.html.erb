<!DOCTYPE html>
<html>
  <head>
    <title>Railsment</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-screen text-gray-800 bg-gray-100">
    <%= render "layouts/header" %>

    <main class="flex flex-col gap-4 grow">
      <%= yield %>
    </main>

    <footer class="text-center bg-gray-800 text-white p-2 text-xs">
      Built with Rails | Released under AGPL
    </footer>
  </body>
</html>
